# {REPO_NAME}

{一句话描述本仓库是什么——L2 适配层？Python 采集客户端？治理文档枢纽？}

{如继承 xlib-standard 规范，写：本仓库遵循 [xlib-standard](https://github.com/ZoneCNH/xlib-standard) 治理协议。}

## 项目概述

{简要说明本仓库的职责、模块、上游依赖。可以用表格。}
{如果是 Go library：列出 public API 面和 driver 位置。}
{如果是 Python client：列出客户端模块和数据源。}

## 仓库结构

```
{关键目录树，不要求完整，突出入口文件}
```

## 构建与测试

{具体命令，可复制粘贴执行：}

```bash
{构建命令}
{测试命令}
{lint 命令}
```

## 硬性约束

{本仓库特有的不可违反规则，例如：}

- {禁止的依赖}
- {模块边界}
- {密钥管理规则}
- {分层约束（如 L2 不能依赖业务 topic）}

## 编辑前基线确认

> 同步自 ZoneCNH/CLAUDE.md 工作流规则。编辑文件前必须先确认当前状态。

- **编辑前先 `git log --oneline -5`，然后 `Read` 确认目标文件当前内容**——禁止假设文件仍是自己记忆中的状态。
- **对文档中的代码事实声称，核对源码后再提交**——用 `grep` 确认字段存在，用 `head` 确认文档不是占位符，用 `git log` 确认文件当前基线。
- **先列验证清单，再列变更清单**——先确定需要查什么，验证完再按变更清单编辑。

## 语言规则（全局强制）

1. **回答语言**：所有对话回复默认使用中文，除非用户明确要求使用其他语言。
2. **文档语言**：所有仓库文档（README、docs/、.agent/、contracts/*.md、变更日志、发布说明、PR 描述、Issue、贡献指南）默认使用中文叙述。
3. **代码注释**：源码中的注释（包括函数文档注释、行内注释、TODO/FIXME）默认使用中文。导出符号的文档注释若面向外部消费者可保留英文，内部代码一律中文。
4. **保留原文的例外**：代码标识符、命令、路径、包名、module 路径、外部专有名词（Agent、Harness、manifest、schema、CI、PR、Issue）、协议固定短语和 git 提交标题保留项目惯用原文。
5. **提交信息**：提交正文（body）和 trailer 使用中文；提交标题（subject line）保留英文以兼容工具链。
