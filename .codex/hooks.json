{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "/usr/bin/env bash -c 'repo=$(git rev-parse --show-toplevel 2>/dev/null) || { printf \"%s\\n\" \"{\\\"continue\\\":true,\\\"stopReason\\\":\\\"auto-delivery: skipped - not a git repo\\\"}\"; exit 0; }; cd \"$repo\" && exec scripts/auto-deliver-on-complete.sh --hook'",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
