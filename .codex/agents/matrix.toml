# 本 agent 已合并到 goal-matrix。请使用 goal-matrix。
# 保留此文件作为指向，避免历史引用断裂。
# 实际执行请 codex --agent goal-matrix
name = "goal-matrix"
description = "已合并到 goal-matrix。请使用 goal-matrix。"
model = "gpt-5.5"
model_reasoning_effort = "high"
