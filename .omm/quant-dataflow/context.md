九条核心设计原则（CONSTITUTION §1 P1-P13 + CLAUDE.md）约束此数据流：策略只通过 riskx 提交订单；回测与实盘共享因子/信号/风控代码；contracts 定义跨域接口；数据不跨域（只通过 L2.5 共享类型）；orderx 抽象交易所差异；x.go 只做编排；域内模块平级协作；反馈通过事件表达；领域语义沉到 L2.5。

分析域 ⇄ 决策域是全系统唯一双向关系：因子驱动信号生成（正向），回测反馈因子评估（反向）。实盘路径与回测路径在 signal_factory 分叉，但共享 factor/signal/risk 代码（P6 回测实盘一致性，§5.5 Mock 契约一致性）。
