FoundationX 三层治理栈的效力层级与 SSOT 边界。最高层是 CONSTITUTION（§0-§20 不可违背的元规则），中间是 governance 操作层（把宪法原则操作化为可执行门禁），底层是 goal 交付 OS（11 阶段管线 + Gate + 状态机）。

三层关系：宪法是 What is inviolable（不可违背什么），governance 是 How to enforce（如何强制），goal 管线是 In what order（按什么顺序交付）。下游文档不得与上游矛盾，冲突时沿链向上回溯。
