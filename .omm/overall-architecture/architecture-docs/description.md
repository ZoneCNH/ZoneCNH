docs/architecture/ 下 8 篇 + ADR 目录，描述 FoundationX 量化系统的完整架构。01-overview（活跃事实链路 SSOT）、02-domain-layers（分层）、03-boundaries（边界）、04-principles（设计原则）、05-foundation（68k 基座详情）、06-dataflow（历史投影，非事实源）、07-three-engines（三引擎，历史）、08-contracts（契约清单）。

根目录 ARCHITECTURE.md 已退化为向后兼容重定向存根，指向 docs/architecture/。
