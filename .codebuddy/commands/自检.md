---
description: "检查 CodeBuddy Code 状态与环境（内置命令 /doctor 的中文别名）"
---

这是内置命令 `/doctor` 的中文别名 `/自检`。

该命令由 CodeBuddy Code 在 CLI 层调度，自定义命令无法直接转发。
请直接运行原来的英文命令来完成操作：

```
/doctor
```

检查 CodeBuddy Code 状态与环境。
