---
description: "管理工具权限与工作区目录访问（内置命令 /permissions 的中文别名）"
---

这是内置命令 `/permissions` 的中文别名 `/权限`。

该命令由 CodeBuddy Code 在 CLI 层调度，自定义命令无法直接转发。
请直接运行原来的英文命令来完成操作：

```
/permissions
```

管理工具权限与工作区目录访问。
