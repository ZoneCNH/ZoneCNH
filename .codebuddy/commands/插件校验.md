---
description: "校验插件目录结构与 manifest（内置命令 /plugin-validate 的中文别名）"
---

这是内置命令 `/plugin-validate` 的中文别名 `/插件校验`。

该命令由 CodeBuddy Code 在 CLI 层调度，自定义命令无法直接转发。
请直接运行原来的英文命令来完成操作：

```
/plugin-validate [path]
```

校验插件目录结构与 manifest。
