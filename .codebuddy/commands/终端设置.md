---
description: "配置 Shift+Enter 换行快捷键（内置命令 /terminal-setup 的中文别名）"
---

这是内置命令 `/terminal-setup` 的中文别名 `/终端设置`。

该命令由 CodeBuddy Code 在 CLI 层调度，自定义命令无法直接转发。
请直接运行原来的英文命令来完成操作：

```
/terminal-setup
```

配置 Shift+Enter 换行快捷键。
