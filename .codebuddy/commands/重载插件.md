---
description: "重新加载所有插件（无需重启）（内置命令 /reload-plugins 的中文别名）"
---

这是内置命令 `/reload-plugins` 的中文别名 `/重载插件`。

该命令由 CodeBuddy Code 在 CLI 层调度，自定义命令无法直接转发。
请直接运行原来的英文命令来完成操作：

```
/reload-plugins
```

重新加载所有插件（无需重启）。
