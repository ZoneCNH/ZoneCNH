---
description: "在当前对话位置创建分支（/branch 别名）（内置命令 /fork 的中文别名）"
---

这是内置命令 `/fork` 的中文别名 `/分叉`。

该命令由 CodeBuddy Code 在 CLI 层调度，自定义命令无法直接转发。
请直接运行原来的英文命令来完成操作：

```
/fork [name]
```

在当前对话位置创建分支（/branch 别名）。
