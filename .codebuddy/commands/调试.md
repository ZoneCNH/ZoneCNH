---
description: "启用调试日志并诊断会话问题（内置命令 /debug 的中文别名）"
---

这是内置命令 `/debug` 的中文别名 `/调试`。

该命令由 CodeBuddy Code 在 CLI 层调度，自定义命令无法直接转发。
请直接运行原来的英文命令来完成操作：

```
/debug [issue description]
```

启用调试日志并诊断会话问题。
