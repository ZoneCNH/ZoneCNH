---
description: "切换或查看图生图模型（内置命令 /model:image-to-image 的中文别名）"
---

这是内置命令 `/model:image-to-image` 的中文别名 `/模型:图生图`。

该命令由 CodeBuddy Code 在 CLI 层调度，自定义命令无法直接转发。
请直接运行原来的英文命令来完成操作：

```
/model:image-to-image [list|model-id]
```

切换或查看图生图模型。
