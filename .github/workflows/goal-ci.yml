# Goal 驱动交付体系 CI/CD
# 权威来源：docs/goal/16-ci-cd.md
# 路径：.github/workflows/goal-ci.yml

name: Goal CI

on:
  workflow_call:
  push:
    branches: [main]
    paths:
      - '.config/goal/**'
      - 'docs/goal/**'
      - 'docs/report/goal-*'
      - '.claude/agents/goal-*.md'
      - '.codex/agents/goal-*'
      - '.copilot/agents/goal-*'
      - 'AGENTS.md'
      - '.github/workflows/goal-ci.yml'
      - '.github/workflows/release.yml'
      - '.github/ci/goal-release-gate.sh'
      - '.github/ci/generate-release-manifest.sh'
  pull_request:
    branches: [main]
    paths:
      - '.config/goal/**'
      - 'docs/goal/**'
      - 'docs/report/goal-*'
      - '.claude/agents/goal-*.md'
      - '.codex/agents/goal-*'
      - '.copilot/agents/goal-*'
      - 'AGENTS.md'
      - '.github/workflows/goal-ci.yml'
      - '.github/workflows/release.yml'
      - '.github/ci/goal-release-gate.sh'
      - '.github/ci/generate-release-manifest.sh'

env:
  GOAL_CI_RUNNER_CLASS: self-hosted
  RUNNER_TOOL_CACHE: ${{ github.workspace }}/.goal-runner-tool-cache
  AGENT_TOOLSDIRECTORY: ${{ github.workspace }}/.goal-runner-tool-cache
  PIP_DISABLE_PIP_VERSION_CHECK: "1"

jobs:
  # 1. YAML 语法检查
  yaml-lint:
    name: YAML Syntax Check
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Set up Goal CI Python toolchain
        run: |
          bash docs/goal/tools/setup-ci-toolchain.sh

      - name: Lint YAML files
        run: |
          yamllint -c .github/yamllint.yml .config/goal/**/*.yaml

  # 2. Registry 完整性检查
  registry-check:
    name: Registry Integrity
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs: yaml-lint
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Set up Goal CI Python toolchain
        run: |
          bash docs/goal/tools/setup-ci-toolchain.sh

      - name: Check Registry files exist
        run: |
          required_files=(
            ".config/goal/registry/goals.yaml"
            ".config/goal/registry/tasks.yaml"
            ".config/goal/registry/issues.yaml"
            ".config/goal/registry/releases.yaml"
            ".config/goal/registry/risks.yaml"
            ".config/goal/registry/decisions.yaml"
          )

          for file in "${required_files[@]}"; do
            if [ ! -f "$file" ]; then
              echo "❌ Missing: $file"
              exit 1
            fi
            echo "✅ Found: $file"
          done

      - name: Validate YAML structure
        run: |
          python3 -c "
          import yaml
          import sys

          files = [
              '.config/goal/registry/goals.yaml',
              '.config/goal/registry/tasks.yaml',
              '.config/goal/registry/issues.yaml',
              '.config/goal/registry/releases.yaml',
              '.config/goal/registry/risks.yaml',
              '.config/goal/registry/decisions.yaml',
              '.config/goal/matrix/matrix.yaml',
              '.config/goal/gates/state.yaml',
              '.config/goal/pipeline/state.yaml',
              '.config/goal/schema/rules.yaml',
          ]

          for f in files:
              try:
                  with open(f) as fh:
                      data = yaml.safe_load(fh)
                  print(f'✅ {f}: valid YAML')
              except Exception as e:
                  print(f'❌ {f}: {e}')
                  sys.exit(1)
          "

  # 3. 规则漂移检查
  rule-drift-check:
    name: Rule Drift Check
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs: registry-check
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Set up Goal CI Python toolchain
        run: |
          bash docs/goal/tools/setup-ci-toolchain.sh

      - name: Validate rule source consistency
        run: |
          python3 docs/goal/tools/rule-drift-check.py --root .

  # 4. Goal 控制面一致性验证
  goal-validator:
    name: Goal Control-Plane Validation
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs: rule-drift-check
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Set up Goal CI Python toolchain
        run: |
          bash docs/goal/tools/setup-ci-toolchain.sh

      - name: Validate Goal control-plane invariants
        run: |
          python3 docs/goal/tools/goal-validate.py --root . --mode strict --format text

  # 5. Goal ID 格式检查
  id-format-check:
    name: Goal ID Format Check
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs: rule-drift-check
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Set up Goal CI Python toolchain
        run: |
          bash docs/goal/tools/setup-ci-toolchain.sh

      - name: Validate registry ID contracts
        run: |
          python3 docs/goal/tools/goal-validate.py --root . --mode strict --only registry --format text

  # 6. Matrix 覆盖率与 edge 完整性检查
  matrix-coverage:
    name: Matrix Coverage Check
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs: rule-drift-check
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Set up Goal CI Python toolchain
        run: |
          bash docs/goal/tools/setup-ci-toolchain.sh

      - name: Check Matrix edge coverage and terminal status
        run: |
          python3 docs/goal/tools/matrix-gen.py --check-only --matrix .config/goal/matrix/matrix.yaml

  # 7. Gate 可执行性检查
  gate-check:
    name: Goal Gate Check
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs:
      - id-format-check
      - matrix-coverage
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Set up Goal CI Python toolchain
        run: |
          bash docs/goal/tools/setup-ci-toolchain.sh

      - name: Run executable Gate checks
        run: |
          bash docs/goal/tools/gate-check.sh .

  # 8. Evidence orphan 闭包检查
  orphan-check:
    name: Evidence Orphan Check
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs: rule-drift-check
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Set up Goal CI Python toolchain
        run: |
          bash docs/goal/tools/setup-ci-toolchain.sh

      - name: Check Evidence and Matrix closure
        run: |
          python3 docs/goal/tools/rule-drift-check.py --root .

  # 9. 统一 Goal 工具链检查
  goal-toolchain-check:
    name: Unified Goal Toolchain Check
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs:
      - goal-validator
      - gate-check
      - orphan-check
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Set up Goal CI Python toolchain
        run: |
          bash docs/goal/tools/setup-ci-toolchain.sh

      - name: Run unified Goal workflow gate
        run: |
          bash docs/goal/tools/goal-workflow.sh gate --root . --mode strict --format text

  # 10. Agent 文件检查
  agent-check:
    name: Agent Files Validation
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs: yaml-lint
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Check Agent files exist
        run: |
          required_agents=(
            ".claude/agents/goal-spec.md"
            ".claude/agents/goal-reviewer.md"
            ".claude/agents/goal-matrix.md"
            ".claude/agents/goal-prompt-builder.md"
            ".claude/agents/goal-evidence.md"
          )

          for agent in "${required_agents[@]}"; do
            if [ ! -f "$agent" ]; then
              echo "❌ Missing: $agent"
              exit 1
            fi
            echo "✅ Found: $agent"
          done

      - name: Validate Agent frontmatter
        run: |
          for agent in .claude/agents/goal-*.md; do
            if head -1 "$agent" | grep -q "^---"; then
              echo "✅ $agent: has frontmatter"
            else
              echo "❌ $agent: missing frontmatter"
              exit 1
            fi
          done

  # 11. 文档一致性检查
  docs-check:
    name: Documentation Consistency
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs: yaml-lint
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - uses: actions/checkout@v4

      - name: Check documentation updates
        run: |
          # Check if docs reference .config/goal/
          if grep -q "统一配置中心" docs/goal/README.md; then
            echo "✅ docs/goal/README.md: references .config/goal/"
          else
            echo "❌ docs/goal/README.md: missing .config/goal/ reference"
            exit 1
          fi

          if grep -q "统一状态路径" docs/goal/15-registry.md; then
            echo "✅ docs/goal/15-registry.md: references .config/goal/"
          else
            echo "❌ docs/goal/15-registry.md: missing .config/goal/ reference"
            exit 1
          fi

          if grep -q "Goal 驱动交付体系" AGENTS.md; then
            echo "✅ AGENTS.md: references Goal system"
          else
            echo "❌ AGENTS.md: missing Goal system reference"
            exit 1
          fi

  # 12. 完整性汇总
  summary:
    name: CI Summary
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 10
    needs:
      - yaml-lint
      - registry-check
      - rule-drift-check
      - goal-validator
      - id-format-check
      - matrix-coverage
      - gate-check
      - orphan-check
      - goal-toolchain-check
      - agent-check
      - docs-check
    if: always()
    steps:
      - name: Prepare self-hosted runner tool cache
        run: |
          mkdir -p "$RUNNER_TOOL_CACHE" "$AGENT_TOOLSDIRECTORY"

      - name: Generate summary
        run: |
          echo "## Goal CI Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| YAML Lint | ${{ needs.yaml-lint.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Registry | ${{ needs.registry-check.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Rule Drift | ${{ needs.rule-drift-check.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Goal Validator | ${{ needs['goal-validator'].result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| ID Format | ${{ needs['id-format-check'].result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Matrix Coverage | ${{ needs['matrix-coverage'].result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Gate Check | ${{ needs['gate-check'].result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Orphan Check | ${{ needs['orphan-check'].result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Goal Toolchain Gate | ${{ needs['goal-toolchain-check'].result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Agent Files | ${{ needs.agent-check.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Documentation | ${{ needs.docs-check.result == 'success' && '✅' || '❌' }} |" >> $GITHUB_STEP_SUMMARY
