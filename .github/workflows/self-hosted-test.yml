name: Self-hosted Test
on: workflow_dispatch

jobs:
  # ── 0. CICD-001 门禁 ───────────────────────────────────────
  runner-gate:
    uses: ./.github/workflows/runner-gate.yml

  test:
    needs: runner-gate
    runs-on: [self-hosted, Linux, X64, sre/governance]
    steps:
      - run: echo "self-hosted runner works" && hostname
