# module/binance schema

模块级数据/API/契约 schema。跨模块 schema SSOT 见 `docs/goal/schema/`。

## 当前状态

待建立。模块级 schema 可包括：
- Binance market event JSON Schema
- API 请求/响应 schema
- 配置 schema（config.yaml 结构定义）

## 与 `docs/goal/schema/` 的关系

`docs/goal/schema/` 定义 Goal 体系通用 schema（goal.schema.yaml、evidence.schema.yaml、matrix.schema.yaml 等）。本目录为 binance 模块专用 schema。
