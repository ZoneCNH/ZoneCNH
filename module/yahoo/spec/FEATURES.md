# yahoo FEATURES

1. 独立 C/S 双服务（`yahoo-client` + `yahoo-server`）。
2. 共享基座能力接入（config/observability/storage/messaging/resilience）。
3. `domain_macro` 语义归一化与 no-lookahead。
4. 七介质链路（taos/kafka/postgres/Redis/oss/nats/clickhouse）。
5. 采集清单 + 更新频率 + 同步周期 + 历史回补策略。

