# Implementation Plans

Generated by the `improve` skill on 2026-06-15 (commit `b4f486b`, deep audit).
Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | 轮换 sre/ 子仓库中所有硬编码凭据 | P1 | S | — | TODO |
| 002  | 修复 audit-status.py 全模式崩溃 | P1 | S | — | DONE |
| 003  | 修复 audit-status.py `or` 短路 bug | P1 | S | 002 | TODO |
| 004  | 重建 release/manifest/latest.json 过期统计 | P1 | M | 002, 003 | TODO |
| 005  | 删除 test_audit_status.py 重复代码 | P2 | S | 002, 003 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **003 requires 002** because both modify `scripts/audit-status.py` in adjacent lines. Complete 002 first, then apply 003 on top to avoid merge conflicts.
- **004 requires 002+003** because the manifest stats must be generated by a correctly-functioning `audit-status.py` (no crash on missing JSON, no short-circuit bug in factory blocker counting).
- **005 suggests after 002+003** to avoid touching the same test file while audit-status.py is being fixed. Not a hard dependency — can be done independently if merge conflicts are managed.

## Findings considered and rejected

- **PERF-07 (CI 零缓存)**: 需要 GitHub Actions 基础设施更改（self-hosted runner 配置），超出了本文档仓库的范围。建议在 `sre/` 仓库中单独处理。
- **PERF-05 (foundation-integration 全串行)**: 同上，CI 架构更改需要 infra 团队介入。
- **PERF-01 (dead validate-write-source job)**: 低优先级，死代码不影响运行时，留待下次 CI 清理时一并处理。
- **DX-03 (O10 Hook 合并)**: 已在 CLAUDE.md 待办清单中，需要 Node.js 重构能力；留待专门的 DX 改进会话。
- **DX-01 (零上手文档)**: 需要项目维护者（ZoneCNH）编写，不适合自动化 agent 执行。
- **TECHDEBT-01 (CLAUDE.md 瘦身)**: 需要人工判断哪些规则应合并/降级/删除，不适合弱模型执行。
- **DIR-01 (O5/O6/O8/O10 基础设施)**: 方向性建议，不是 bug。保留在方向发现中供维护者参考。
- **SEC-04 (DeepSeek API token)**: `settings.local.json` 已被 `.gitignore` 保护，且此文件是用户本地配置；提请注意但不在本文档仓库范围内修复。
- **DOCS-03 (decimalx 版本不一致)**: 单行修复，trivial 程度不适合独立计划。建议在下一个 STATUS.md 维护 PR 中顺手修正。
- **TESTS-06/07 (JS/Shell 脚本零测试)**: 工作量 L，需要测试框架引入。建议在补齐 Python 测试后再考虑。

## Audit summary

Deep audit of ZoneCNH/ZoneCNH documentation repository (666 markdown files, 17 scripts, 13 CI workflows, 9 hooks). 8 parallel subagents covered correctness, security, performance, test coverage, tech debt, dependencies, DX, docs, and direction. **~80 findings total**, 5 selected for implementation plans.

### Key risks identified

1. **CRITICAL**: Hardcoded credentials in `sre/` sub-repo (PAT, DB passwords, SSH keys) — Plan 001
2. **HIGH**: `audit-status.py` crashes in full mode when `.foundationx/` JSON files are missing — Plan 002
3. **HIGH**: `release/manifest/latest.json` all 8 stats stale — Plan 004
4. **MEDIUM**: 43/80 modules without version tags, zero CI caching, CLAUDE.md at 462 lines (32% over target)

### Not audited

- `sre/` repository internals beyond secrets and SSH keys (separate git repo)
- External 404 scanning for all 84 ZoneCNH repos (requires `--network` flag, not run)
- Module SPEC.md content quality beyond section count compliance
- Historical audit trails in `docs/solutions/` and `docs/evidence/`
